/** @jsx jsx */
import { jsx } from "@emotion/react";
import { PropsWithChildren } from "react";
declare type FormRowProps = {
    hasError?: boolean;
};
export default function FormRow({ children, hasError }: PropsWithChildren<FormRowProps>): jsx.JSX.Element;
export {};
